/** @format */

import { expect } from "@playwright/test";

import { customTest as test } from "../../fixtures/custom-test";

test("Banner IS visible when 6 events are returned", async ({
  loginAndGoToEvents,
}) => {});
